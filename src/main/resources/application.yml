spring:
  datasource:
    url: jdbc:postgresql://172.16.0.36:5432/postgres
    username: postgres
    password: postgres
  jpa:
    generate-ddl: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update